---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Contact Us">
  
  <!-- Hero -->
  <section class="relative py-24 bg-gradient-to-br from-primary-600 via-winter-600 to-primary-700 overflow-hidden">
    <div class="absolute inset-0">
      <div class="absolute -top-40 -right-40 w-80 h-80 bg-white/10 rounded-full blur-3xl"></div>
      <div class="absolute -bottom-40 -left-40 w-80 h-80 bg-white/10 rounded-full blur-3xl"></div>
    </div>
    
    <div class="relative container mx-auto px-4 sm:px-6 lg:px-8 text-center">
      <div class="max-w-4xl mx-auto space-y-6">
        <h1 class="text-5xl md:text-6xl font-extrabold text-white">
          Get Your Free Quote
        </h1>
        <p class="text-xl md:text-2xl text-white/90">
          Ready to experience worry-free winter? Let's talk about your snow removal needs.
        </p>
      </div>
    </div>
  </section>

  <!-- Contact Section -->
  <section class="py-24 bg-gray-50">
    <div class="container mx-auto px-4 sm:px-6 lg:px-8">
      <div class="grid grid-cols-1 lg:grid-cols-5 gap-12 max-w-7xl mx-auto">
        
        <!-- Contact Info -->
        <div class="lg:col-span-2 space-y-8">
          <div>
            <h2 class="text-3xl font-bold text-gray-900 mb-6">Get in Touch</h2>
            <p class="text-gray-600 leading-relaxed">
              Contact us today for a free, no-obligation quote tailored to your property's specific needs.
            </p>
          </div>

          <!-- Contact Cards -->
          <div class="space-y-6">
            <div class="bg-white rounded-2xl p-6 shadow-lg hover:shadow-xl transition-shadow">
              <div class="flex items-start space-x-4">
                <div class="w-12 h-12 bg-gradient-to-br from-primary-500 to-winter-500 rounded-xl flex items-center justify-center flex-shrink-0">
                  <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z" />
                  </svg>
                </div>
                <div>
                  <h3 class="font-bold text-gray-900 mb-1">Phone</h3>
                  <a href="tel:4165557669" class="text-primary-600 font-semibold hover:underline">(416) 555-SNOW</a>
                  <p class="text-sm text-gray-500 mt-1">24/7 during winter storms</p>
                </div>
              </div>
            </div>

            <div class="bg-white rounded-2xl p-6 shadow-lg hover:shadow-xl transition-shadow">
              <div class="flex items-start space-x-4">
                <div class="w-12 h-12 bg-gradient-to-br from-winter-500 to-primary-500 rounded-xl flex items-center justify-center flex-shrink-0">
                  <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z" />
                  </svg>
                </div>
                <div>
                  <h3 class="font-bold text-gray-900 mb-1">Email</h3>
                  <a href="mailto:info@blizzardwizard.ca" class="text-primary-600 font-semibold hover:underline">info@blizzardwizard.ca</a>
                  <p class="text-sm text-gray-500 mt-1">Response within 24 hours</p>
                </div>
              </div>
            </div>

            <div class="bg-white rounded-2xl p-6 shadow-lg hover:shadow-xl transition-shadow">
              <div class="flex items-start space-x-4">
                <div class="w-12 h-12 bg-gradient-to-br from-primary-500 to-winter-500 rounded-xl flex items-center justify-center flex-shrink-0">
                  <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z" />
                  </svg>
                </div>
                <div>
                  <h3 class="font-bold text-gray-900 mb-1">Office Hours</h3>
                  <p class="text-gray-600 text-sm">Mon-Fri: 8AM - 6PM</p>
                  <p class="text-gray-600 text-sm">Sat: 9AM - 4PM</p>
                  <p class="text-gray-600 text-sm">Sun: Emergency only</p>
                </div>
              </div>
            </div>
          </div>

          <!-- Emergency Box -->
          <div class="bg-gradient-to-br from-primary-600 to-winter-600 rounded-2xl p-6 text-white">
            <h3 class="text-xl font-bold mb-2">❄️ Emergency Snow Removal</h3>
            <p class="text-white/90 text-sm leading-relaxed">
              Need immediate assistance? We offer priority emergency service during major snowfall events. Call us for rapid response.
            </p>
          </div>
        </div>

        <!-- Contact Form -->
        <div class="lg:col-span-3">
          <div class="bg-white rounded-3xl shadow-2xl p-8 md:p-10">
            <h3 class="text-3xl font-bold text-gray-900 mb-8">Request Your Free Quote</h3>
            
            <form class="space-y-6" x-data="{ submitted: false }" @submit.prevent="submitted = true; setTimeout(() => alert('Thank you! In production, this would send your information to our team. We\\'ll contact you within 24 hours.'), 100)">
              
              <div>
                <label class="block text-sm font-semibold text-gray-700 mb-2">Full Name *</label>
                <input type="text" required class="w-full px-4 py-3 border-2 border-gray-200 rounded-xl focus:border-primary-500 focus:ring-2 focus:ring-primary-200 outline-none transition-colors" placeholder="John Smith">
              </div>

              <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                <div>
                  <label class="block text-sm font-semibold text-gray-700 mb-2">Email *</label>
                  <input type="email" required class="w-full px-4 py-3 border-2 border-gray-200 rounded-xl focus:border-primary-500 focus:ring-2 focus:ring-primary-200 outline-none transition-colors" placeholder="john@example.com">
                </div>
                <div>
                  <label class="block text-sm font-semibold text-gray-700 mb-2">Phone *</label>
                  <input type="tel" required class="w-full px-4 py-3 border-2 border-gray-200 rounded-xl focus:border-primary-500 focus:ring-2 focus:ring-primary-200 outline-none transition-colors" placeholder="(416) 555-0000">
                </div>
              </div>

              <div>
                <label class="block text-sm font-semibold text-gray-700 mb-2">Property Address *</label>
                <input type="text" required class="w-full px-4 py-3 border-2 border-gray-200 rounded-xl focus:border-primary-500 focus:ring-2 focus:ring-primary-200 outline-none transition-colors" placeholder="123 Main Street">
              </div>

              <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                <div>
                  <label class="block text-sm font-semibold text-gray-700 mb-2">City *</label>
                  <select required class="w-full px-4 py-3 border-2 border-gray-200 rounded-xl focus:border-primary-500 focus:ring-2 focus:ring-primary-200 outline-none transition-colors">
                    <option value="">Select City</option>
                    <option value="north-york">North York</option>
                    <option value="richmond-hill">Richmond Hill</option>
                    <option value="markham">Markham</option>
                    <option value="vaughan">Vaughan</option>
                    <option value="scarborough">Scarborough</option>
                    <option value="other">Other</option>
                  </select>
                </div>
                <div>
                  <label class="block text-sm font-semibold text-gray-700 mb-2">Service Type *</label>
                  <select required class="w-full px-4 py-3 border-2 border-gray-200 rounded-xl focus:border-primary-500 focus:ring-2 focus:ring-primary-200 outline-none transition-colors">
                    <option value="">Select Service</option>
                    <option value="seasonal">Seasonal Contract</option>
                    <option value="per-visit">Per Visit Service</option>
                    <option value="emergency">Emergency Removal</option>
                    <option value="custom">Custom Package</option>
                  </select>
                </div>
              </div>

              <div>
                <label class="block text-sm font-semibold text-gray-700 mb-2">Additional Information</label>
                <textarea rows="4" class="w-full px-4 py-3 border-2 border-gray-200 rounded-xl focus:border-primary-500 focus:ring-2 focus:ring-primary-200 outline-none transition-colors resize-none" placeholder="Tell us about your property size, special requirements, or any questions..."></textarea>
              </div>

              <div class="flex items-start space-x-3">
                <input type="checkbox" class="mt-1 w-5 h-5 text-primary-600 border-gray-300 rounded focus:ring-2 focus:ring-primary-200">
                <label class="text-sm text-gray-600">
                  Send me winter weather alerts and service updates
                </label>
              </div>

              <button type="submit" class="w-full px-8 py-4 bg-gradient-to-r from-primary-600 to-winter-600 text-white font-bold rounded-full text-lg shadow-lg hover:shadow-2xl transform hover:scale-105 transition-all duration-300">
                Get My Free Quote
              </button>

              <p class="text-sm text-gray-500 text-center">
                * Required fields. We respect your privacy and will never share your information.
              </p>
            </form>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- FAQ -->
  <section class="py-20 bg-white">
    <div class="container mx-auto px-4 sm:px-6 lg:px-8">
      <h2 class="text-4xl font-bold text-center text-gray-900 mb-12">Frequently Asked Questions</h2>
      
      <div class="grid grid-cols-1 md:grid-cols-2 gap-6 max-w-5xl mx-auto">
        {[
          { q: "How quickly can you provide a quote?", a: "Most quotes are provided within 24 hours. During peak season, up to 48 hours." },
          { q: "Do you offer same-day service?", a: "Yes! For emergency situations, we offer same-day service when available." },
          { q: "What payment methods do you accept?", a: "We accept cash, e-transfer, and all major credit cards. Seasonal contracts can be paid in installments." },
          { q: "Do I need to be home during service?", a: "No! Most customers are not home. We'll notify you when we're coming and when the job is complete." }
        ].map((item) => (
          <div class="bg-gray-50 rounded-2xl p-6 hover:bg-gray-100 transition-colors">
            <h4 class="text-lg font-bold text-gray-900 mb-2">{item.q}</h4>
            <p class="text-gray-600">{item.a}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

</Layout>
